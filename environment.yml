name: chesstorch
channels:
  - defaults
dependencies:
  - pytorch::pytorch
  - torchaudio
  - torchvision
  - ultralytics
prefix: /Users/akash/miniconda3/envs/chesstorch
# do conda config --append channels conda-forge before running conda env create -f environment.yml